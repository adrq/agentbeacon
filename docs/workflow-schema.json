{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agentmaestro.dev/workflow-schema.json",
  "title": "AgentMaestro Workflow Definition",
  "description": "Validates the AgentMaestro workflow YAML structure once converted to JSON.",
  "type": "object",
  "required": ["name", "tasks"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Human readable name displayed in dashboards and logs."
    },
    "description": {
      "type": "string",
      "description": "Optional summary presented to workflow authors."
    },
    "config": {
      "type": "object",
      "description": "Workflow-wide configuration map applied to every task.",
      "additionalProperties": true
    },
    "on_error": {
      "type": "string",
      "enum": ["stop_all", "continue_branches"],
      "description": "Global failure strategy selected for the workflow."
    },
    "tasks": {
      "type": "array",
      "description": "Ordered list of tasks executed sequentially or in parallel.",
      "minItems": 1,
      "items": { "$ref": "#/$defs/TaskDefinition" }
    }
  },
  "$defs": {
    "Identifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[a-z0-9_-]+$",
      "description": "Slug identifier used for dependency references."
    },
    "TaskDefinition": {
      "type": "object",
      "required": ["id", "agent", "task"],
      "additionalProperties": false,
      "properties": {
        "id": { "$ref": "#/$defs/Identifier" },
        "agent": {
          "type": "string",
          "minLength": 1,
          "description": "Agent identifier defined in repository configuration."
        },
        "task": {
          "type": "object",
          "description": "Payload delivered to the selected agent. For A2A agents, conforms to A2A v0.3.0 protocol with required 'history' field.",
          "required": ["history"],
          "properties": {
            "history": {
              "type": "array",
              "minItems": 1,
              "items": { "$ref": "a2a-v0.3.0.schema.json#/definitions/Message" },
              "description": "Message history per A2A spec. First message is the user prompt."
            },
            "artifacts": {
              "type": "array",
              "items": { "$ref": "a2a-v0.3.0.schema.json#/definitions/Artifact" },
              "description": "Optional artifacts passed to agent."
            },
            "contextId": {
              "type": "string",
              "description": "Optional context ID for grouping related tasks."
            },
            "metadata": {
              "type": "object",
              "additionalProperties": true,
              "description": "Protocol-specific metadata forwarded to agent."
            }
          }
        },
        "inputs": {
          "$ref": "#/$defs/TaskInputs",
          "description": "Declarative inputs required before the task is executed."
        },
        "depends_on": {
          "type": "array",
          "description": "Optional list of prerequisite task identifiers.",
          "items": { "$ref": "#/$defs/Identifier" },
          "uniqueItems": true
        },
        "execution": {
          "$ref": "#/$defs/ExecutionPolicy",
          "description": "Overrides for timeout and retry behaviour."
        }
      },
      "unevaluatedProperties": false
    },
    "TaskInputs": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "artifacts": {
          "type": "array",
          "description": "Artifacts that must be available before the task runs.",
          "items": { "$ref": "#/$defs/InputArtifactReference" },
          "uniqueItems": true,
          "minItems": 1
        }
      }
    },
    "InputArtifactReference": {
      "type": "object",
      "description": "Reference to an artifact emitted by an upstream task.",
      "required": ["from", "artifactId"],
      "additionalProperties": false,
      "properties": {
        "from": {
          "$ref": "#/$defs/Identifier",
          "description": "Identifier of the upstream task that produced the artifact."
        },
        "artifactId": {
          "$ref": "#/$defs/Identifier",
          "description": "Artifact identifier declared on the producing task."
        }
      }
    },
    "ExecutionPolicy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "timeout": {
          "type": "integer",
          "minimum": 0,
          "description": "Seconds allowed for the task to run before a timeout is raised."
        },
        "retry": {
          "$ref": "#/$defs/RetryPolicy",
          "description": "Retry policy that overrides the workflow default."
        }
      }
    },
    "RetryPolicy": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "attempts": {
          "type": "integer",
          "minimum": 1,
          "default": 1,
          "description": "Maximum number of attempts including the original execution."
        },
        "backoff": {
          "type": "string",
          "enum": ["fixed", "linear", "exponential"],
          "description": "Retry backoff strategy applied between attempts."
        },
        "delay_seconds": {
          "type": "integer",
          "minimum": 0,
          "description": "Delay in seconds between retry attempts when applicable."
        }
      }
    }
  }
}
