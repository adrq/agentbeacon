agents:
  # Simple stdio agent for testing
  mock-agent:
    type: stdio
    config:
      command: "./bin/mock-agent"

  # Example A2A agent configuration
  # orchestrator-remote:
  #   type: a2a
  #   config:
  #     url: "http://remote:9456/"
  #     timeout: 300

  # Example ACP agent configuration (future implementation)
  # gemini-prod:
  #   type: acp
  #   config:
  #     command: "gemini"
  #     args: ["--model", "gemini-2.5-pro"]
  #     env:
  #       GEMINI_API_KEY: ${GEMINI_API_KEY}
