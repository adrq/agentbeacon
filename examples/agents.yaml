agents:
  # Simple stdio agent for testing
  mock-agent:
    type: stdio
    config:
      command: "./bin/mock-agent"

  # Example A2A agent configuration
  # orchestrator-remote:
  #   type: a2a
  #   config:
  #     url: "http://remote:9456/"
  #     timeout: 300

  # Example ACP agent configuration
  # gemini-cli:
  #   type: acp
  #   config:
  #     command: "gemini-cli"
  #     args: ["--api-key", "${GEMINI_API_KEY}", "--model", "gemini-2.0-flash-exp"]

  # Example ACP agent with mock-agent for testing
  # acp-mock:
  #   type: acp
  #   config:
  #     command: "./bin/mock-agent"
  #     args: ["--mode", "acp"]
